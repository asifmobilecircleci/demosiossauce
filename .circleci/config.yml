version: 2.1

jobs:
  build:
    macos:
      xcode: 14.2
    environment:
      SAUCE_USERNAME: oauth-asifbillionbus786-7d468
      SAUCE_ACCESS_KEY: 4a919a9f-cea3-48cc-b58b-645ab4442d7e
    steps:
      - checkout
      - run:
          name: Install Fastlane
          command: bundle install
      - run:
          name: Build and run tests
          command: bundle exec fastlane build_and_test
      - run:
          name: Upload app and test runner to Sauce Labs
          command: bundle exec fastlane upload_to_sauce

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
